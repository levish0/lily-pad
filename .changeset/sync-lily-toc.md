---
"@levish0/lily-pad": patch
---

Sync the table of contents with lily-svelte so active-item emphasis scales the label instead of animating the link's font size and padding, reducing layout shifts while scrolling.
