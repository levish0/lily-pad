import { type UserConfig } from 'velite';
/** lily-pad's velite setup: a single `docs` collection over `content/**\/*.md`
 *  with the frontmatter schema the theme understands. Use as the default
 *  export of `velite.config.js`. */
export declare function docsConfig(): UserConfig;
